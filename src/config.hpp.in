#pragma once

#define CLOCKS_NAME L"${NAME}"
#define CLOCKS_VERSION L"${VERSION}"
#define CLOCKS_DESCRIPTION L"${DESCRIPTION}"
#define CLOCKS_AUTHOR L"${AUTHOR}"
#define CLOCKS_C_SYMBOL L"${C_SYMBOL}"
#define CLOCKS_COPYRIGHT L"${COPYRIGHT}"
#define CLOCKS_URL L"${URL}"

#define CLOCKS_VERSION_MAJOR ${VERSION_MAJOR}
#define CLOCKS_VERSION_MINOR ${VERSION_MINOR}
#define CLOCKS_VERSION_PATCH ${VERSION_PATCH}
